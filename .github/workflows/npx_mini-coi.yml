name: npx_mini-coi
on:
  push:
  #  branches: [main]
  pull_request:
  #  branches: [main]
jobs:
  npx_mini-coi:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npx mini-coi .
